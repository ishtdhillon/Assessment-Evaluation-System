{
  "name": "result-management",
  "version": "1.0.0",
  "description": "Result workflow portal with a lightweight Node backend, SQLite persistence, and backend-verified auth.",
  "main": "server.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "check": "node --check app.js && node --check server.js",
    "test": "npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.102.1"
  }
}
